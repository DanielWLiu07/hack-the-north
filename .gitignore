# Secrets — ignored from the very first commit
.env
.env.local
*.pem
*.key

# The room's own repository is a SEPARATE repo, never nested in this one
room.git/

# Data
clouds/*.ply
*.rrd
__pycache__/
*.pyc
.venv/
venv/

# Calibration outputs are generated, but DO commit the final extrinsics.yaml
perception/calib/*.npz

.DS_Store

# Local caches, credentials, and generated captures
.pytest_cache/
node_modules/
.env.*
!.env.example
*.log
*.rrd

# sentry_watch.py: downloaded failure photos (of the room) + its bookkeeping
evidence/
.sentry_watch_seen.json

# local working notes for individual workstreams; not part of the project
web/landing/HANDOFF-3D.md
web/landing/HANDOFF-SEER-INTRO.md
web/landing/HANDOFF-ROOM-PANE.md
web/pages/seer/HANDOFF-SEER-PANE.md

# Live camera frames and the map the page draws from them: runtime data, and the robot's
# camera sees whoever is in the room. Never in the repo; the deploy excludes live/ too.
web/landing/live/
.handover/
